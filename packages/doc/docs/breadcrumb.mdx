---
id: breadcrumb
title: Breadcrumb 面包屑
---

import { Breadcrumb } from '@rexd/core';
import * as stories from 'story/src/breadcrumb.stories';

## 使用场景

用来告知用户当前的位置，以及当前页面在整个网站中的位置，属于一种辅助的导航方式，适用于清晰且具多层次结构的网站，每一层内容为层级归属关系，方便用户返回上一级或各个层级的页面。

## 示例

### 基本示例

<Story fn={stories.Basic} />

### 自定义分隔线

<Story fn={stories.CustomSeparator} />

## API

<PropsTable component={Breadcrumb} />
