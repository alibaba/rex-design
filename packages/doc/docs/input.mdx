---
id: input
title: Input 输入框
---

import { Input } from '@rexd/core';
import * as stories from 'story/src/input.stories';

## 使用场景

输入框，可以用来输入剪短的文本内容。

## 示例

### 基本示例

<Story fn={stories.Basic} />

### 校验状态

通过 status 传入输入框的校验状态

<Story fn={stories.Status} />

### 清空

通过 hasClear 设置是否显示输入框清空按钮

<Story fn={stories.HasClear} />

### 无边框的输入框

通过 shape 设置简洁输入框，没有边框

<Story fn={stories.NoBorder} />

### 内部元素

使用 leftElement, rightElement 传入内部的扩展内容

<Story fn={stories.Elements} />

### 关闭自动填充

可以通过 autoComplete="off" 属性关闭文本框的自动填充，但由于[某些原因](https://developer.mozilla.org/zh-CN/docs/Web/Security/Securing_your_site/Turning_off_form_autocompletion#%E8%87%AA%E5%8A%A8%E5%A1%AB%E5%85%85%E5%B1%9E%E6%80%A7%E5%92%8C%E7%99%BB%E5%BD%95)，现代浏览器大多不支持该设置。如果设置不生效，建议参考[该文档](https://developer.mozilla.org/zh-CN/docs/Web/Security/Securing_your_site/Turning_off_form_autocompletion)。

<Story fn={stories.AutoCompleteOff} />

### 输入框组合

将输入框和其他元素进行组合

<Story fn={stories.Addons} />

## API

<PropsTable component={Input} />
