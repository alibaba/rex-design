---
id: button
title: Button 按钮
---

import { Button } from '@rexd/core';
import { Basic, FullWidth, Size, Group, AddIcon, Loading } from 'story/src/button.stories';

## 使用场景

标记了一个（或封装一组）操作命令，响应用户点击行为，触发相应的业务逻辑。

## 示例

### 基本示例

<Story fn={Basic} />

### 链接型按钮

与文本按钮相比，链接型按钮没有内间距

TODO

`<Story fn={LinkButton} />`

### 按钮大小

通过 size 设置按钮的尺寸

<Story fn={Size} />

### 整行按钮

通过 isFullWidth 设置按钮占据整行，通常用于小屏场景

<Story fn={FullWidth} />

### 按钮中的图标

<Story fn={AddIcon} />

### 按钮组

TODO

`<Story fn={ButtonGroup} />`

### 加载状态

<Story fn={Loading} />

### 可选中的按钮

TODO

`<Story fn={Toggle} />`

`<Story fn={ToggleGroup} />`

## API

<PropsTable component={Button} />
