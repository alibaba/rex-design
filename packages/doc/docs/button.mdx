---
id: button
title: Button 按钮
---

import { Button } from '@rexd/core';
import {
  basic,
  fullWidth,
  size,
  group,
  addIcon,
  loading,
  shape,
} from 'story/src/button.stories';

## 使用场景

标记了一个（或封装一组）操作命令，响应用户点击行为，触发相应的业务逻辑。

## 示例

### 基本示例

<Story fn={basic} />

### 整行按钮

<Story fn={fullWidth} />

### 按钮大小

<Story fn={size} />

### 按钮组

<Story fn={group} />

### 按钮中的图标

<Story fn={addIcon} />

### 加载状态

<Story fn={loading} />

### 不同形状

<Story fn={shape} />

## API

<PropsTable component={Button} />
