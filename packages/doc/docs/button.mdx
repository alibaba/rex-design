---
id: button
title: Button 按钮
---

import { Button } from '@rexd/core';
import * as stories from 'story/src/button.stories';

## 使用场景

标记了一个（或封装一组）操作命令，响应用户点击行为，触发相应的业务逻辑。

## 示例

### 基本示例

<Story fn={stories.Basic} />

### 链接型按钮

与文本按钮相比，链接型按钮没有内间距

<Story fn={stories.LinkButton} />

### 按钮大小

通过 size 设置按钮的尺寸

<Story fn={stories.Size} />

### 整行按钮

通过 isFullWidth 设置按钮占据整行，通常用于小屏场景

<Story fn={stories.FullWidth} />

### 带图标的按钮

<Story fn={stories.AddIcon} />

### 图标按钮

通过 isIconButton 设置按钮为图标按钮，此时按钮的宽高相同。

<Story fn={stories.OnlyIcon} />

### 按钮组

<Story fn={stories.ButtonGroup} />

### 加载状态

<Story fn={stories.Loading} />

### 可选中的按钮

<Story fn={stories.Toggle} />

## API

<PropsTable component={Button} />
